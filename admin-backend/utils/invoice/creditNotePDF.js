export async function generateCreditNote(order, returnedItems) {
  // same as invoice but:
  // title: "CREDIT NOTE"
  // reference original invoice no
  // negative amount
}
//Return approved → generate credit note → refund